version: 2

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gofmt
    - goimports
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused

linters-settings:
  gofmt:
    simplify: true
  goimports:
    local-prefixes: dupclean

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
